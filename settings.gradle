pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}
rootProject.name = 'DepSchedule'

include 'backend', 'schedule_ui',
        'backend:openapi_codegen',
        'backend:liquibase'

