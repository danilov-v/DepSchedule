export const getStartDateSelector = state => state.scheduler.startDate;

export const getEndDateSelector = state => state.scheduler.endDate;

export const getOperationalDateSelector = state =>
  state.scheduler.operationalDate;

export const getUnitsSelector = state => state.scheduler.units;

export const getPeriodsSelector = state => state.scheduler.periods;

export const getEventsSelector = state => state.scheduler.events;
